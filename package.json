{
  "name": "nuntius-website",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build --base=/nuntius/ && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "vite": "^2.9.0"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
